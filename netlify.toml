# Publish directory
[build]
  publish = "public"

# Headers for APK files
[[headers]]
  for = "/*.apk"
  [headers.values]
    Content-Type = "application/vnd.android.package-archive"
    Content-Disposition = "attachment"
    Cache-Control = "public, max-age=31536000, immutable"
